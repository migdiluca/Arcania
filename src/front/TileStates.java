package front;

/**
 * Enum que representa los distintos estados en los que se puede encontrar un determinado Tile
 */

public enum TileStates {
    INACTIVE, ACTIVE, MOVABLE, INVOKABLE, ATTACKABLE;
}
